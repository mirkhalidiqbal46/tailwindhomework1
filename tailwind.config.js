/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./*.html", // Include all HTML files in the root directory
    "./src/**/*.{html,js}", // Include HTML and JS files in the `src` folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
